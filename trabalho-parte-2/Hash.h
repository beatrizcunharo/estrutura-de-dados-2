#ifndef HASH_H_INCLUDED
#define HASH_H_INCLUDED
#include "Dados_Hash.h"
#include "Data.h"

#include <vector>

class Hash
{
/*
    public:

        Hash(int N)
        {
            tamanho = N;
            dados = new vector<Dados_Hash>[tamanho];
        }

        ~Hash()
        {
            // DESTRUTOR VAZIO
        }

        // OUTRAS FUNÇÕES HASH

        string substituirPontos(std::string string, const std::string& atual, const std::string& proximo);

        int funcaoHash(string app_review);

        void inserir(Data data);

        vector<Dados_Hash> *getDados()
        {
            return this->dados;
        };

    private:
        int tamanho;
        vector<Dados_Hash> *dados;
        */
};

#endif // HASH_H_INCLUDED